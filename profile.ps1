#!/usr/bin/env powershell
#
# Powershell init script
# Check possible locations via `$PROFILE | Select-Object *`
#

# use readline/emacs/bash keybindings
# github.com/PowerShell/PSReadLine
Set-PSReadLineOption -EditMode Emacs
